<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="linear_layout">
        <item name="android:paddingTop">@dimen/activity_vertical_margin</item>
        <item name="android:paddingBottom">@dimen/activity_vertical_margin</item>
        <item name="android:paddingRight">@dimen/activity_vertical_margin</item>
        <item name="android:paddingLeft">@dimen/activity_vertical_margin</item>
    </style>

    <style name="button_style" parent="Base.Widget.AppCompat.Button">
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_margin">@dimen/button_margin</item>
        <item name="android:background">@drawable/buttons_selector</item>
        <item name="android:padding">@dimen/button_padding</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="simple_notification_style" parent="button_style">
        <item name="android:text">@string/simple_notification_button_text</item>
    </style>

    <style name="clear_notification_style" parent="button_style">
        <item name="android:text">@string/cancel_notification_button_text</item>
    </style>
</resources>
